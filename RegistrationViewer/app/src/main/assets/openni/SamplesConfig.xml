<OpenNI>
	<Licenses>
		<!-- Add application-specific licenses here 
		<License vendor="vendor" key="key"/>
		-->
	</Licenses>
	<Log writeToConsole="false" writeToFile="false" writeToAndroidLog="true">
		<!-- 0 - Verbose, 1 - Info, 2 - Warning, 3 - Error (default) -->
		<LogLevel value="0"/>
		<Masks>
			<Mask name="ALL" on="true"/>
		</Masks>
	</Log>
	<ProductionNodes>
		<!-- Uncomment following line, in order to run from a recording 
		<Recording file="sampleRec.oni" />
		-->
	
		<!-- Set global mirror -->
		<GlobalMirror on="true"/>
		
		<!-- Create a depth node and give it a name alias (useful if referenced ahead in this script) -->
		<!-- <Node type="Depth" name="MyDepth">
			<Configuration>
				<MapOutputMode xRes="640" yRes="480" FPS="30" />
			</Configuration>
		</Node> -->

		<!-- Create an image node. If it fails, continue anyway.
    	<Node type="Image" name="MyImage" stopOnError="false">
        	<Configuration>
            	<MapOutputMode xRes="320" yRes="240" FPS="30" />
        	</Configuration>
    	</Node> -->
			
		<!-- Uncomment nodes from here if you need them.
		<Node type="Audio" />
		<Node type="User" />
		<Node type="Hands" />
		<Node type="Gesture" />
		<Node type="Scene" />
		-->
	</ProductionNodes>
</OpenNI>
